config BR2_PACKAGE_MDNSD
	bool "mdnsd"
	depends on BR2_TOOLCHAIN_HAS_THREADS
	help
	  mDNS responder - server for device discovery

comment "mdnsd needs a toolchain w/ threads"
	depends on !BR2_TOOLCHAIN_HAS_THREADS
